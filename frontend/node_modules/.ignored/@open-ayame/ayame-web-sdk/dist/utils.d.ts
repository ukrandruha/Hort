/**
 * ブラウザを判定する
 */
export declare function browser(): string;
/**
 * デバッグログを出力する
 */
export declare function traceLog(title: string, value?: string | Record<string, any>): void;
/**
 * 指定された codec にマッチする codec のリストを返す
 * リストなのはプロファイルが複数合ったり、 RTX, RED, ULPFEC などの codec も含めるため
 */
export declare const getSelectedCodecs: (kind: "audio" | "video", selectedCodecMimeType: string, codecs: RTCRtpCodecCapability[]) => RTCRtpCodecCapability[];
/**
 * 利用可能な映像のコーデックを取得する
 */
export declare const getAvailableCodecs: (kind: "audio" | "video", direction: "sender" | "receiver") => string[];
